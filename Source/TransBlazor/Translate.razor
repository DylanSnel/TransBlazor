@typeparam TGlobal
@typeparam TLocal

@if(Default is not null)
{
    @Default
}

@code {


    [Parameter] public required string Key { get; set; }


    [CascadingParameter] public required TGlobal Global { get; set; }
    [CascadingParameter] public required TLocal Local { get; set; }
    [CascadingParameter] public string ComponentName { get; set; } = string.Empty;

    RenderFragment? Loading { get; set; }
    RenderFragment? Default { get; set; }

}